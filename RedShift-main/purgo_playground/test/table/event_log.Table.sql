
CREATE TABLE purgo_playground.event_log (
	eventid VARCHAR(256), 
	version VARCHAR(256), 
	task VARCHAR(256), 
	systemtime VARCHAR(256), 
	eventrecordid VARCHAR(256), 
	subjectusername VARCHAR(256), 
	subjectdomainname VARCHAR(256), 
	objecttype VARCHAR(256), 
	ipaddress VARCHAR(256), 
	ipport VARCHAR(256), 
	sharename VARCHAR(256), 
	sharelocalpath VARCHAR(256), 
	accesslist VARCHAR(256), 
	objectname VARCHAR(256)
)

