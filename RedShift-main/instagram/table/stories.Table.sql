
CREATE TABLE instagram.stories (
	id BIGINT, 
	taps_forward INTEGER, 
	taps_back INTEGER, 
	replies INTEGER, 
	reach INTEGER, 
	exits INTEGER, 
	follows INTEGER, 
	"total interactions" INTEGER, 
	shares INTEGER
)

